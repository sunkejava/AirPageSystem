# AirPageSystem v0.0.6

## 修复

- 修复全局授权策略错误保护 Vue SPA fallback，导致直接访问或刷新前端页面返回 HTTP 401。
- 公开 `index.html`、静态资源、客户端路由 fallback 和版本接口；业务数据 API 继续强制登录。
- 会话失效时前端自动返回登录页，并提取标准 Problem Details 错误信息，不再把空白 401 当作页面内容。

## UI 与结构优化

- 重新设计登录页、侧栏、顶部栏、统计卡片、模板卡片、表格、表单、墨水屏预览和移动端布局。
- 新增统一色彩、间距、阴影、交互状态及响应式视觉规范，保持本地离线运行，不依赖外部字体或 CDN。
- 将登录、导航和顶部栏拆分为独立 Vue 组件，导航定义与面板预设拆入配置模块。
- 前端版本号改为从后端 `/api/version` 动态读取，避免前后端版本不一致。

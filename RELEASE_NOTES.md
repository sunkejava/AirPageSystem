# AirPageSystem v0.0.5

## 新增

- 增加登录、用户、角色、菜单权限和数据级隔离；设备、数据源、任务、私有模板及推送记录默认仅归属创建用户。
- 每位用户可维护自己的默认设备；存在多个设备时，预览、即时推送和定时任务均可选择目标设备。
- 增加可配置重试规则，支持生成重试、推送重试、最大次数、初始延迟、指数退避和最大延迟。
- 增加安全 JSON 面板绘制功能，内置金句、电子工牌、登机牌、自由布局及图片自动适配入口。
- 前端界面和后端启动日志显示当前版本号。

## 修复与优化

- 修复模板未绑定数据源时把空字符串提交到 `Guid?` 字段导致保存返回 HTTP 400。
- 依据 3x-ui v3.7.0 数据模型修正客户端与入站统计语义，兼容 Xray v26.7.28，增加出站流量、启用/到期客户端、最近活跃、配额及独立运行时长。
- 字体集合和字号对象改为单例缓存，关闭 Server GC，显著减少重复渲染分配并降低小内存服务器的工作集压力。
- 旧版数据库自动补齐用户归属、重试规则及权限相关表，无需删除 `data` 目录。

首次升级登录账号为 `admin`，一次性随机密码仅显示在本机首次启动日志中。也可通过 `BootstrapAdmin__Password` 环境变量预设，登录后请立即修改。

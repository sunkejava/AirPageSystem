# AirPageSystem v0.0.4

## 修复

- 修复 Windows 自包含版本错误地把 `.ttc` 字体集合当作单字体加载，导致所有预览和推送返回 HTTP 500。
- 修复定时任务在面板生成失败后缺少可见执行结果的问题。
- 调度器启动后立即检查到期任务，并在外部推送前持久化下次执行时间，避免遗漏或重复执行。
- 增强 Windows/Linux 时区兼容与 Cron、模板、设备引用校验。

## 新增

- 定时任务支持编辑、删除、启停和测试执行。
- 定时任务列表展示下次执行时间、上次执行时间及真实执行结果。
- 面板模板支持编辑名称和描述；内置模板保留固定渲染类型，避免误改后失效。
- Release 说明从本版本开始仅包含当前版本变更。

升级时保留原 `data` 目录即可，无需删除数据库或重新添加 AirPage 设备。
